package info

// Version is the application version (normally set when building the binary).
var Version = "dev"

// AppID is the identifier of the app, a name that can be used
// to now who is executing the logic of the app/runtime.
const AppID = "stactus"
